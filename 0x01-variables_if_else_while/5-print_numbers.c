#include <stdio.h>
/**
 * main - progam that prints all single digit numbers of base 10
 * return: Always 0.
 */
int main(void)
{
	int num;
	while (num < 10)
{
	printf("%d", num)
	num += 1;
}
	putchar 
	return (0)
}
